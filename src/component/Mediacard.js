import React from 'react'

export const Mediacard = ({title, body}) => {
    return (
        <div>
            <h2>title {title}</h2>
            <p>body       {body}</p>
            <image src=""/>
        </div>
    )
}
